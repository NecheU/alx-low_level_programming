#include <stdio.h>
#include <stdlib.h>
#include <time>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
low char;
for (low = 'a'; low <= 'z'; low++)
putchar(low);
putchar(low = 'A'; low <= 'Z'; low++)
putchar(low);
putchar('\n');

return (0);
}
