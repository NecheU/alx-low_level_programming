#include "main.h"
/**
 * _abs - prints tve absolute value of a num
 * @i: the number to be evaluated
 *
 * Return: 1,0 or -1
 */
int _abs(int i)
{
	if (i > 0)
	return (1);
	else if (i < 0)
		return (-1);
	else
		return (0);
}
