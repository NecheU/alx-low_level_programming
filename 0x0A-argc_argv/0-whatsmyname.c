#include <stdio.h>

/**
 * main - C program that prints its name
 * @argc: arguement count
 * @argv: arguement vector
 *
 * Return: 0
 */
int main(int __attribute__ ((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
